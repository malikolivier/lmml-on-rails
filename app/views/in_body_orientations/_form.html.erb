<%= form_for(in_body_orientation) do |f| %>
  <% if in_body_orientation.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(in_body_orientation.errors.count, "error") %> prohibited this in_body_orientation from being saved:</h2>

      <ul>
      <% in_body_orientation.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :coordinate_system %>
    <%= f.number_field :coordinate_system %>
  </div>

  <div class="field">
    <%= f.label :x %>
    <%= f.text_field :x %>
  </div>

  <div class="field">
    <%= f.label :y %>
    <%= f.text_field :y %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
