<%= form_for(test_histopathology) do |f| %>
  <% if test_histopathology.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(test_histopathology.errors.count, "error") %> prohibited this test_histopathology from being saved:</h2>

      <ul>
      <% test_histopathology.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :note %>
    <%= f.text_area :note %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
