<p id="notice"><%= notice %></p>

<p>
  <strong>Title:</strong>
  <%= @explication.title %>
</p>

<p>
  <strong>Content:</strong>
  <%= @explication.content %>
</p>

<%= link_to 'Edit', edit_explication_path(@explication) %> |
<%= link_to 'Back', explications_path %>
