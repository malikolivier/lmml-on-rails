<%= form_for(explication) do |f| %>
  <% if explication.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(explication.errors.count, "error") %> prohibited this explication from being saved:</h2>

      <ul>
      <% explication.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %>
    <%= f.text_area :title %>
  </div>

  <div class="field">
    <%= f.label :content %>
    <%= f.text_area :content %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
