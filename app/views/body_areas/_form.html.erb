<%= form_for(body_area) do |f| %>
  <% if body_area.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(body_area.errors.count, "error") %> prohibited this body_area from being saved:</h2>

      <ul>
      <% body_area.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :distance %>
    <%= f.text_field :distance %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
