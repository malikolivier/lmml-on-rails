<%= form_for(injury) do |f| %>
  <% if injury.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(injury.errors.count, "error") %> prohibited this injury from being saved:</h2>

      <ul>
      <% injury.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :description %>
    <%= f.text_area :description %>
  </div>

  <div class="field">
    <%= f.label :when_alive %>
    <%= f.number_field :when_alive %>
  </div>

  <div class="field">
    <%= f.label :note %>
    <%= f.text_area :note %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
