<%= form_for(body_reference) do |f| %>
  <% if body_reference.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(body_reference.errors.count, "error") %> prohibited this body_reference from being saved:</h2>

      <ul>
      <% body_reference.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_area :name %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
