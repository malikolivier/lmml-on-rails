<%= form_for(injury_depth) do |f| %>
  <% if injury_depth.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(injury_depth.errors.count, "error") %> prohibited this injury_depth from being saved:</h2>

      <ul>
      <% injury_depth.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :depth %>
    <%= f.text_field :depth %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
