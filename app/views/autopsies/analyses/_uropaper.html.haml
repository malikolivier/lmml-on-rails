= t('analysis_uropapers.description')

%table
  %tbody
    - exam.uropaper_results.each do |result|
      - next if result.no_result?
      %tr
        %td= result.translated_category
        %td= result.descriptive_result
    - if exam.ph.present?
      %tr
        %td= t('activerecord.attributes.analysis_uropaper.ph')
        %td= exam.ph
