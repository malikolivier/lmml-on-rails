= exam.description

- if exam.triage_drug_results.not_positive.any?
  %table
    %thead
      %tr
        %th 薬物
        %th 検出限界濃度(ng/mL)
    %tbody
      - exam.triage_drug_results.includes(drug: [:translations, :triage_supported_drug]).not_positive.each do |result|
        %tr
          %td= result.drug.description
          %td= result.drug.triage_concentration_threshold
