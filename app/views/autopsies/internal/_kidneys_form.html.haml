- left = true
= render 'primary_dual_form', exam: exam do |f|
  = f.number_field :weight
  = f.smart_select :hardness
  = f.smart_select :capsule_removal
  = f.smart_select :capsule_congestion
  = f.smart_select :pelvis_congestion
  = f.smart_select :pelvis_petechia
  - if left
    - # Only show common fields (examination note) once
    = yield f
    - left = false
