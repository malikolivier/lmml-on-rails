= form_for(exam, builder: VueFormBuilder, html: { id: 'internal_thoracic_aorta_examination' }, url: '') do |f|
  = f.hidden_field :id
  = f.number_field :width
  = f.smart_select :arteriosclerosis
  = yield f
