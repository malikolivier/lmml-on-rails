= form_for(exam, builder: VueFormBuilder, html: { id: 'external_anus_examination' }, url: '') do |f|
  = f.hidden_field :id
  .row
    .col-xs-6
      = f.smart_select :closed
    .col-xs-6
      = f.smart_select :feces_quantity
  = yield f
