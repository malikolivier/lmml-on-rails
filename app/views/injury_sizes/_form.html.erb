<%= form_for(injury_size) do |f| %>
  <% if injury_size.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(injury_size.errors.count, "error") %> prohibited this injury_size from being saved:</h2>

      <ul>
      <% injury_size.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :shape %>
    <%= f.number_field :shape %>
  </div>

  <div class="field">
    <%= f.label :length %>
    <%= f.text_field :length %>
  </div>

  <div class="field">
    <%= f.label :width %>
    <%= f.text_field :width %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
