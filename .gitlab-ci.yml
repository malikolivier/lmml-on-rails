before_script:
  - apt-get update -qq && apt-get install -y -qq sqlite3 libsqlite3-dev nodejs npm
  - ruby -v
  - which ruby
  - gem install bundler --no-ri --no-rdoc
  - bundle install --jobs $(nproc)  "${FLAGS[@]}"
  - npm install

rubocop:
  script:
    - bundle exec rubocop

rails_test:
  script:
    - bundle exec rails test

js_lint:
  script:
    - npm test

js_test:
  script:
    - bundle exec teaspoon
